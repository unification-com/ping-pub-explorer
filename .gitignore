node_modules/
**/.vscode
yarn-error.log
dist
.idea

.env*
!.env.example
!.env.production

# Unification fork: local-only devnet chain configs (loaded via VITE_CHAINS_ALLOWLIST
# during `yarn dev` against a local docker devnet). Not for production deploy.
chains/*/*-devnet.json
