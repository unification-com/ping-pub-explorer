# Production build: deployed Unification explorer only shows the Unification
# chain entry from chains/{mainnet,testnet}/*.json. Other upstream chain JSONs
# are kept in-tree so future upstream rebases don't conflict on file deletions
# (see src/stores/useDashboard.ts for the runtime filter).
VITE_CHAINS_ALLOWLIST=u
